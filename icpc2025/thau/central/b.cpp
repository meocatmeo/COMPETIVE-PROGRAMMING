#include<bits/stdc++.h>
using namespace std;
#define ll long long
int n , m, v;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin >> n >> m >> v;
    for (int i = 1;i <=m ;i++){
        int a, b;
        ll d;
        cin >> a >> b >> d;
        
    }
    return 0;
}